# Required
SECRET_KEY="your-secure-secret-key"

# Environment (local or production)
# Set to "production" to use AWS DynamoDB, leave unset or set to "local" for local JSON database
FLASK_ENV="local"

# AWS Region
AWS_REGION="us-east-2"

# AWS Credentials (optional — only needed for local development)
# On AWS Elastic Beanstalk / EC2, IAM roles are used automatically.
#AWS_ACCESS_KEY_ID="your-aws-access-key"
#AWS_SECRET_ACCESS_KEY="your-aws-secret-key"
