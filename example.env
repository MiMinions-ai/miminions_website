# Required Environment Variables for AWS Elastic Beanstalk
# Configure these in EB Console > Configuration > Software > Environment properties

# Flask Security
SECRET_KEY="your-secure-secret-key"

# AWS Configuration (EB uses IAM roles, but these are needed for DynamoDB)
AWS_REGION="us-east-2"
AWS_DEFAULT_REGION="us-east-2"
#AWS_ACCESS_KEY_ID="your-aws-access-key"
#AWS_SECRET_ACCESS_KEY="your-aws-secret-key"
# These keys are commented out because AWS Elastic Beanstalk typically uses IAM roles for permissions.
# Use AmazonDynamoDBFullAccess roles for the EB instance profile.
